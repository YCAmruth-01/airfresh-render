services:
  - type: web
    name: airfresh-backend
    runtime: python
    buildCommand: pip install -r airfresh-backend/requirements.txt
    startCommand: cd airfresh-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0